<!-- Loop over each object in the projects array -->
{{#each posts as |post|}}
  <div>
    <div>
      <h2>
        <a href='/post/{{post.id}}'>{{post.title}}</a>
      </h2>
    </div>
    <div>
      <p>
        {{post.content}}
      </p>
      <p>Posted by {{post.user.username}} on {{format_date post.date_posted}}</p>
    </div>
  </div>
{{/each}}